<div class="row">
 <div class="col-md-8 col-md-offset-2 table-responsive">
     <table class="table table-striped table-bordered table-hover">
        <thead>
         <tr>
             <th class="logiccs_table">Sytem-id</th>
             <th class="logiccs_table">Company</th>
             <th class="logiccs_table">Name</th>
             <th class="logiccs_table">Email</th>
             <th class="logiccs_table">Operation</th>
         </tr>
        </thead>
        <tbody>
            <% @dealerusers.each do |user| %>
            <tr>
                <td> <%= user.id %></td>
                <td> <%= user.dealercompany.name %></td>
                <td> <%= user.name %></td>
                <td> <%= user.email %></td>
                <td>
                  <%= link_to "Edit", edit_dealeruser_path(user), class:"btn btn-success btn-xs" %>
                  <%= link_to "Delete", user, method: 'delete', class:"btn btn-danger btn-xs" %>
                </td>
            </tr>
            <% end %>
            
        </tbody>
         
         
         
         
     </table>
  </div>
</div>